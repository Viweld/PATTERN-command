import 'package:flutter/material.dart';

class GarageDoor extends StatelessWidget {
  const GarageDoor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
